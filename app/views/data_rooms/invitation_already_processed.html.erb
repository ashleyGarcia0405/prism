<div class="min-h-screen bg-slate-900 flex items-center justify-center py-12 px-4">
  <div class="max-w-md w-full bg-slate-800 rounded-lg shadow-xl p-8 text-center">
    <svg class="mx-auto h-16 w-16 text-blue-400 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>

    <h1 class="text-2xl font-bold text-white mb-2">Invitation Already Processed</h1>
    <p class="text-slate-300 mb-6">
      This invitation has already been <%= @invitation.status %>. You cannot take any further action on it.
    </p>

    <% if @invitation.status == "accepted" %>
      <%= link_to "View Data Room", data_room_path(@invitation.data_room_id), class: "inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition mr-2" %>
    <% end %>

    <%= link_to "Return to Dashboard", root_path, class: "inline-block bg-slate-700 hover:bg-slate-600 text-white font-semibold px-6 py-3 rounded-lg transition" %>
  </div>
</div>