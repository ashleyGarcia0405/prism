<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #e2e8f0;
      background-color: #0f172a;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #1e293b;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
    }
    .header {
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      padding: 30px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      color: white;
      font-size: 24px;
      font-weight: 600;
    }
    .content {
      padding: 40px 30px;
    }
    .greeting {
      font-size: 18px;
      color: #f1f5f9;
      margin-bottom: 20px;
    }
    .message {
      color: #cbd5e1;
      margin-bottom: 20px;
      line-height: 1.7;
    }
    .data-room-details {
      background-color: #0f172a;
      border-left: 4px solid #3b82f6;
      padding: 20px;
      margin: 25px 0;
      border-radius: 4px;
    }
    .data-room-details h3 {
      margin: 0 0 10px 0;
      color: #f1f5f9;
      font-size: 16px;
    }
    .data-room-details p {
      margin: 5px 0;
      color: #94a3b8;
      font-size: 14px;
    }
    .data-room-details .label {
      color: #64748b;
      font-weight: 500;
    }
    .button-container {
      text-align: center;
      margin: 35px 0;
    }
    .button {
      display: inline-block;
      padding: 14px 32px;
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      font-size: 16px;
      box-shadow: 0 2px 4px rgba(59, 130, 246, 0.3);
    }
    .button:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
    }
    .secondary-info {
      margin-top: 30px;
      padding-top: 30px;
      border-top: 1px solid #334155;
      color: #94a3b8;
      font-size: 14px;
    }
    .footer {
      background-color: #0f172a;
      padding: 20px;
      text-align: center;
      color: #64748b;
      font-size: 12px;
    }
    .expires-warning {
      background-color: #422006;
      border-left: 4px solid #f59e0b;
      padding: 12px 16px;
      margin: 20px 0;
      border-radius: 4px;
      color: #fbbf24;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🔒 Data Room Invitation</h1>
    </div>

    <div class="content">
      <p class="greeting">Hello <%= @organization.name %> Team,</p>

      <p class="message">
        <strong><%= @invited_by.name %></strong> from <strong><%= @invited_by.organization.name %></strong>
        has invited you to participate in a secure multi-party computation data room.
      </p>

      <div class="data-room-details">
        <h3><%= @data_room.name %></h3>
        <% if @data_room.description.present? %>
          <p><%= @data_room.description %></p>
        <% end %>
        <p style="margin-top: 15px;">
          <span class="label">Query Type:</span>
          <%= @data_room.query_type&.upcase || 'Not specified' %>
        </p>
        <% if @data_room.query_text.present? %>
          <p>
            <span class="label">Query:</span>
            <%= @data_room.query_text %>
          </p>
        <% end %>
      </div>

      <p class="message">
        This collaboration uses secure multi-party computation to ensure your data remains private
        while enabling joint analytics. Your data never leaves your control.
      </p>

      <div class="expires-warning">
        ⏰ This invitation expires on <%= @invitation.expires_at.strftime("%B %d, %Y at %I:%M %p") %>
      </div>

      <div class="button-container">
        <%= link_to "Accept Invitation", @accept_url, class: "button" %>
      </div>

      <div class="secondary-info">
        <p><strong>Next Steps:</strong></p>
        <ol style="color: #cbd5e1; margin: 10px 0; padding-left: 20px;">
          <li>Click the button above to review the invitation</li>
          <li>Select which dataset you'd like to contribute</li>
          <li>Attest to the query to confirm your participation</li>
          <li>Once all participants attest, the computation will be executed</li>
        </ol>
      </div>

      <div class="secondary-info">
        <p style="margin: 5px 0;">
          If you have questions about this invitation, please contact <%= @invited_by.email %>.
        </p>
        <p style="margin: 5px 0;">
          If you did not expect this invitation, you can safely ignore this email.
        </p>
      </div>
    </div>

    <div class="footer">
      <p>Prism - Differential Privacy Platform</p>
      <p>Secure Multi-Party Computation</p>
    </div>
  </div>
</body>
</html>