<nav class="bg-slate-800 shadow-lg mb-6 border-b border-slate-700">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <div class="flex items-center space-x-4">
        <%= link_to root_path, class: "text-2xl font-bold text-blue-400 hover:text-blue-300" do %>
          Prism
        <% end %>
        <span class="text-sm text-gray-400">Privacy-Preserving Analytics</span>
      </div>

      <div class="flex items-center space-x-6">
        <%= link_to "Dashboard", dashboard_path, class: "text-gray-300 hover:text-blue-400 transition" %>
        <%= link_to "Datasets", datasets_path, class: "text-gray-300 hover:text-blue-400 transition" %>
        <%= link_to "Queries", queries_path, class: "text-gray-300 hover:text-blue-400 transition" %>
        <%= link_to "Data Rooms", data_rooms_path, class: "text-gray-300 hover:text-blue-400 transition" %>

        <div class="border-l border-slate-700 pl-6">
          <span class="text-gray-300"><%= current_user.name %></span>
          <span class="text-gray-500 text-sm">(<%= current_user.organization.name %>)</span>
          <%= button_to "Logout", logout_path, method: :delete, class: "ml-4 text-red-400 hover:text-red-300 transition" %>
        </div>
      </div>
    </div>
  </div>
</nav>